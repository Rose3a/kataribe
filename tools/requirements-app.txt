# Torch is installed separately from the selected CPU/CUDA index.
transformers==5.16.1
huggingface-hub==1.30.0
safetensors==0.8.0
sentencepiece==0.1.99
soundfile==0.14.0
pyyaml==6.0.3
einops==0.8.2
descript-audiotools==0.7.2
numpy==2.4.6
torch==2.10.0
torchaudio==2.10.0
torchvision==0.25.0
torchao>=0.16,<0.17
torchcodec>=0.10,<0.11
# 口パクのタイムライン（/irodori/timeline）で使うASR。モデル本体（約626MB）は
# 初回利用時にエンジンが models\asr へ自動取得する。
sherpa-onnx>=1.12,<2
# ONNX packages are installed separately by setup.ps1.  descript-audiotools
# requires protobuf<3.20, while modern ONNX requires protobuf>=3.20.2.
